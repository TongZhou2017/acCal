# acCal 项目设置指南

## ✅ 已完成的工作

1. ✅ GitHub Pages Jekyll 工作流已配置
2. ✅ Jekyll 项目结构已创建
3. ✅ 自动化脚本已更新（输出到 `_conferences` 目录）
4. ✅ 基础页面和布局已创建

## 📋 下一步操作清单

### 1. ✅ 配置文件已更新

所有配置文件中的占位符已替换为 `TongZhou2017`：

- ✅ `_config.yml` - 已更新 `social.github` 和导航链接
- ✅ `index.md` - 已更新提交会议链接
- ✅ `_pages/conferences.md` - 已更新提交会议链接
- ✅ `_pages/about.md` - 已更新所有 GitHub 链接
- ✅ `README.md` - 已更新提交会议链接

### 2. 配置 GitHub Pages

1. 前往仓库设置：`Settings` → `Pages`
2. 确认 Source 设置为：`Deploy from a branch`
3. 选择 Branch：`main`，文件夹：`/ (root)`
4. 点击 `Save`

### 3. 测试本地 Jekyll（可选）

如果你想在本地预览网站：

```bash
# 安装依赖
bundle install

# 启动本地服务器
bundle exec jekyll serve

# 访问 http://localhost:4000
```

### 4. 测试自动化流程

1. 创建一个测试 Issue：
   - 前往 `Issues` → `New Issue`
   - 选择 `conference_submission.yml` 模板
   - 填写测试数据并提交

2. 检查 GitHub Actions：
   - 前往 `Actions` 标签页
   - 查看 `Issue 自动转换为内容PR` 工作流是否运行
   - 查看是否自动创建了 PR

3. 审核并合并 PR：
   - 检查生成的 Markdown 文件
   - 确认无误后合并 PR
   - 网站将自动更新

### 5. 自定义网站外观（可选）

- 修改 `assets/css/main.css` 调整样式
- 修改 `_layouts/default.html` 调整布局
- 可以安装 Jekyll 主题（需要更新 `_config.yml`）

## 📁 项目结构说明

```
acCal/
├── .github/
│   ├── ISSUE_TEMPLATE/
│   │   └── conference_submission.yml  # Issue 提交模板
│   └── workflows/
│       ├── jekyll-gh-pages.yml        # GitHub Pages 构建工作流
│       └── process_issue.yml          # Issue 转 PR 工作流
├── _conferences/                      # 会议内容集合（Jekyll）
│   └── .gitkeep
├── _layouts/                          # Jekyll 布局文件
│   ├── default.html                   # 默认布局
│   └── conference.html                # 会议详情页布局
├── _pages/                            # 静态页面
│   ├── conferences.md                 # 会议列表页
│   └── about.md                       # 关于页面
├── assets/
│   └── css/
│       └── main.css                   # 主样式文件
├── scripts/
│   └── issue_to_md.py                 # Issue 转 Markdown 脚本
├── _config.yml                        # Jekyll 配置文件
├── Gemfile                            # Ruby 依赖文件
├── index.md                           # 首页
└── README.md                          # 项目说明
```

## 🚀 部署流程

1. **提交代码到 GitHub**
   ```bash
   git add .
   git commit -m "feat: 初始化 Jekyll 网站结构"
   git push origin main
   ```

2. **GitHub Actions 自动构建**
   - 推送后，`jekyll-gh-pages.yml` 会自动运行
   - 构建完成后，网站会在几分钟内上线

3. **访问网站**
   - URL: `https://tongzhou2017.github.io/acCal/`
   - 或自定义域名（如已配置）

## 🔧 常见问题

### Q: 网站没有更新？
A: 检查 GitHub Actions 是否成功运行，查看是否有错误日志。

### Q: 会议列表页面显示为空？
A: 确保 `_conferences` 目录中的文件 `draft: false`，或者修改 `_pages/conferences.md` 中的过滤条件。

### Q: 本地预览失败？
A: 确保已安装 Ruby 和 Bundler，运行 `bundle install` 安装依赖。

## 📝 注意事项

- 所有会议文件已自动设置为 `draft: false`，合并 PR 后即可在网站上显示
- 确保 GitHub Actions 有权限创建 PR（默认 `GITHUB_TOKEN` 通常足够）
- 如果使用自定义域名，需要在 `_config.yml` 中更新 `url` 和 `baseurl`

